import random_generator

def generate_random_numbers(start, end, num_values):
  """
  This function generates a list of random numbers within a specified range.

  Args:
      start: The starting number of the range (inclusive).
      end: The ending number of the range (inclusive).
      num_values: The number of random numbers to generate.

  Returns:
      A list of random integers within the specified range.
  """

